# Meeting Bot Requirements (Docker-optimized)
# Excludes local audio libraries (pyaudio, webrtcvad, sounddevice)
# as meeting bot uses Recall.ai for audio I/O

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
asyncio>=3.4.3

# Recall.ai Meeting Bot Integration
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
redis>=5.0.0
pydantic>=2.9.0
aiosqlite>=0.20.0
httpx>=0.27.0

# Minimal audio processing (no system dependencies)
numpy>=1.24.0

# UI (optional)
rich>=13.7.0
colorama>=0.4.6

# Performance monitoring
psutil>=5.9.0
